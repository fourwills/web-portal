# Default production API (used by Vercel when env vars are not set in dashboard)
VITE_API_BASE_URL=https://portal.incorpus.in/api_dnl/v1
VITE_DEV_MOCK_AUTH=false

# Our platform IPs (comma-separated, optional :port) — not fetched from API
VITE_PLATFORM_IPS=163.172.118.64
